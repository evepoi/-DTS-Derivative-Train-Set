/*--------------------------------------------------------------------
스위치
--------------------------------------------------------------------*/
// 편성그래픽
switch (FEAT_TRAINS, SELF, sw_NJ2_engine_1Car, position_in_vehid_chain % 1) {
    set_NJ2_engine; //default
    }

// 속도변수
switch(FEAT_TRAINS, SELF, sw_NJ2_speed, Use_max_virtual_speed) {
    1: return Regular_Speed_Train;
    return 120;
    }